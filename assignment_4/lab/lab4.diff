diff -ur coreutils-with-bug/lib/timespec.h coreutils-without-bug/lib/timespec.h
--- coreutils-with-bug/lib/timespec.h	2019-10-31 16:37:30.871124000 -0700
+++ coreutils-without-bug/lib/timespec.h	2019-10-31 16:35:58.563627000 -0700
@@ -45,8 +45,29 @@
 static inline int
 timespec_cmp (struct timespec a, struct timespec b)
 {
-  int diff = a.tv_sec - b.tv_sec;
-  return diff ? diff : a.tv_nsec - b.tv_nsec;
+  if (a.tv_sec - b.tv_sec < 0)
+    {
+      return -1;
+    }
+  else if (a.tv_sec - b.tv_sec > 0)
+    {
+      return 1;
+    }
+  else
+    {
+      if (a.tv_nsec - b.tv_nsec < 0)
+	{
+	  return -1;
+	}
+      else if (a.tv_nsec - b.tv_nsec > 0)
+	{
+	  return 1;
+	}
+      else
+	{
+	  return 0;
+	}
+    }
 }
 
 # if ! HAVE_DECL_NANOSLEEP
diff -ur coreutils-with-bug/lib/timespec.h~ coreutils-without-bug/lib/timespec.h~
--- coreutils-with-bug/lib/timespec.h~	2019-10-31 13:20:39.180428000 -0700
+++ coreutils-without-bug/lib/timespec.h~	2019-10-31 16:36:00.152634000 -0700
@@ -68,6 +68,8 @@
 	  return 0;
 	}
     }
+  # int diff = a.tv_sec - b.tv_sec;
+  # return diff ? diff : a.tv_nsec - b.tv_nsec;
 }
 
 # if ! HAVE_DECL_NANOSLEEP
